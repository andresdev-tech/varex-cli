{
  "name": "varex-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Varex CLI monorepo",

  "packageManager": "pnpm@11.1.1",

  "engines": {
    "node": ">=22.0.0"
  },

  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "clean": "turbo run clean"
  },

  "keywords": [
    "varex",
    "cli",
    "typescript",
    "monorepo"
  ],

  "author": "andres",
  "license": "MIT",

  "devDependencies": {
    "turbo": "^2.5.6",
    "typescript": "^5.9.3"
  },

  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}