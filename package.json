{
  "name": "varex-workspace",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@11.1.1",

  "engines": {
    "node": ">=22.0.0"
  },

  "scripts": {
    "dev:cli": "turbo run dev --filter=@varex/cli",
    "build:all": "turbo run build",
    "start:cli": "turbo run start --filter=@varex/cli",
    "clean": "turbo run clean"
  },

  "devDependencies": {
    "turbo": "^2.10.0",
    "typescript": "^5.9.3"
  }
}